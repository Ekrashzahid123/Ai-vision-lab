# CRITICAL: Headless OpenCV ONLY - List FIRST to prevent GUI version conflict
# The GUI version (opencv-python) will cause libGL.so.1 errors on Streamlit Cloud
opencv-python-headless>=4.5.2

# Core ML Framework 
# NOTE: Do not specify torchvision with opencv-python as dependency
ultralytics==8.4.92
streamlit==1.53.0

# PyTorch - Install without opencv-python
torch>=2.0.0

# Data & Visualization
pillow>=9.0.0
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.5.0
pyyaml>=5.4.0



